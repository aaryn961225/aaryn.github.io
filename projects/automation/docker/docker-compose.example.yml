version: "3.9"
services:
  qa-automation:
    build:
      context: ..
      dockerfile: Dockerfile
    environment:
      TEST_BASE_URL: ${TEST_BASE_URL:-http://host.docker.internal:3000}
      AI_REVIEW_MODE: mock
    volumes:
      - ../reports:/work/reports
      - ../playwright-report:/work/playwright-report
